set Dumbell (struct [
    weight: Number
]);
set Cat (struct [
    cuteness: Number
]);
set Building (struct [
    height: Number
]);
def lift [this: Dumbell, strength: Number] -> Unknown { print "oof"; print this.weight; 0 };

print (+ 1 2);
print (+ "hi " "wow");