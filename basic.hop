let Dumbell (struct [
    weight: Number
]);
let Cat (struct [
    cuteness: Number
]);
let Building (struct [
    height: Number
]);
def lift [this: Dumbell, strength: Number] -> Unknown { print "oof"; print this.weight; 0 };

print (+ 1 2);
print (+ "hi " "wow");