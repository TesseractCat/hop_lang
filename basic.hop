set Dumbell (struct [
    weight: Number
]);
set Cat (struct [
    cuteness: Number
]);
set Building (struct [
    height: Number
]);
def lift [this: Dumbell, strength: Number] -> Number { print "oof"; print this.weight; 0 };